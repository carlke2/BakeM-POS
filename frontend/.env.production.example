# Server build — copy to frontend/.env on the production machine (not committed)
VITE_API_URL=https://betterfork.millenium.co.ke/api
VITE_SOCKET_URL=https://betterfork.millenium.co.ke
# Scanner stays on each school PC, not on the cloud server
VITE_FINGERPRINT_SCANNER_URL=http://127.0.0.1:17890
